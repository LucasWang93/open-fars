Title: Tool-R0: Self-Evolving LLM Agents for Tool-Learning from Zero Data

Abstract: Large language models (LLMs) are becoming the foundation for autonomous agents that can use tools to solve complex tasks. Reinforcement learning (RL) has emerged as a common approach for injecting such agentic capabilities, but typically under tightly controlled training setups. It often depends on carefully constructed task-solution pairs and substantial human supervision, which creates a fundamental obstacle to open-ended self-evolution toward superintelligent systems. In this paper, we propose Tool-R0 framework for training general purpose tool-calling agents from scratch with self-play RL, under a zero-data assumption. Initialized from the same base LLM, Tool-R0 co-evolves a Generator and a Solver with complementary rewards: one proposes targeted challenging tasks at the other's competence frontier and the other learns to solve them with real-world tool calls. This creates a self-evolving cycle that requires no pre-existing tasks or datasets. Evaluation on different tool-use benchmarks show that Tool-R0 yields 92.5 relative improvement over the base model and surpasses fully supervised tool-calling baselines under the same setting. Our work further provides empirical insights into self-play LLM agents by analyzing co-evolution, curriculum dynamics, and scaling behavior.

Body: Tool-R0: Self-Evolving LLM Agents for Tool-Learning from Zero Data 1 Introduction 2 Related Work 3 Method 3.1 Grounded Task Specification. 3.2 Generator Training 3.2.1 Generator Reward Design Format Reward ( r fmt r_{\text{fmt}} ): Tags and Parseability. Validity Reward ( r valid r_{\text{valid}} ): Available Tools, Gold-Calls, and Value Grounding. Curriculum Reward ( r curr r_{\text{curr}} ): Difficulty Semantic Alignment. 3.3 Solver Dataset Construction 3.4 Solver Training Accuracy Reward ( r acc r_{\text{acc}} ). 4 Experiments Models. Training Details. Evaluation. 4.1 Results 4.2 Ablation Studies and Analysis 5 Discussion 5.1 Conclusion 5.2 Challenges 5.3 What is Next A Related Work Extended Tool Learning with LLMs. Self-Play in LLMs. B Grounded Task Specification B.1 Formal Definitions B.2 Domain Configurations B.3 Sampling Strategy and Hyperparameters Domain sampling. Hyperparameters for Task Specifications. C Generator Implementation Details C.1 Training Setup C.2 Reward Hyperparameters Structured output interface. Reward composition. Difficulty estimation. Semantic alignment. Normalization. D Details of Solver Dataset Construction D.1 Specification-Grounded Generation and Deduplication D.2 Solver-Based Cross-Verification D.3 Difficulty Probing and Curriculum Selection E Solver Implementation Details E.1 Training Setup E.2 Reward Hyperparameters Output interface and parsing. Format reward ( r fmt r_{\text{fmt}} ). Accuracy reward ( r acc r_{\text{acc}} ). Robust value comparison. Penalty for extra tool calls. F Further Details on Evaluation G Training Details of Supervised Baseline Tool-Calling Agents H Full Algorithm of Tool-R0 Tool-R0 : Self-Evolving LLM Agents for Tool-Learning from Zero Data Emre Can Acikgoz 1 1 , Cheng Qian 1 1 , Jonas Hübotter 2 2 , Heng Ji 1 1 , Dilek Hakkani-Tür 1 1 , Gokhan Tur 1 1 1 1 UIUC 2 2 ETH Zurich Abstract Large language models (LLMs) are becoming the foundation for autonomous agents that can use tools to solve complex tasks. Reinforcement learning (RL) has emerged as a common approach for injecting such agentic capabilities, but typically under tightly controlled training setups. It often depends on carefully constructed task–solution pairs and substantial human supervision, which creates a fundamental obstacle to open-ended self-evolution toward superintelligent systems. In this paper, we propose Tool-R0 framework for training general purpose tool-calling agents from scratch with self-play RL, under a zero-data assumption . Initialized from the same base LLM, Tool-R0 co-evolves a Generator and a Solver with complementary rewards: one proposes targeted challenging tasks at the other’s competence frontier and the other learns to solve them with real-world tool calls. This creates a self-evolving cycle that requires no pre-existing tasks or datasets. Evaluation on different tool-use benchmarks show that Tool-R0 yields 92.5% relative improvement over the base model and surpasses fully supervised tool-calling baselines under the same setting. Our work further provides empirical insights into self-play LLM agents by analyzing co-evolution, curriculum dynamics, and scaling behavior. Code Project Page Models Logs 1 Introduction Large language models (LLMs) have demonstrated remarkable capabilities in complex reasoning tasks (Jaech et al. , 2024 ; Guo et al. , 2025 ; Team et al. , 2025 ) . A key driver of this progress is reinforcement learning with verifiable rewards (RLVR), which has emerged as a powerful post-training paradigm for improving reasoning performance (Lambert et al. , 2025 ) . These advances position LLMs as promising language agents that can reason and act in real-world environments to accomplish goals by interacting with external tools (Yao et al. , 2023 ; Qu et al. , 2025 ) . To teach LLMs with these agentic capabilities, large-scale RLVR has become the focal point (Zhang et al. , 2025a ; Qian et al. , 2025 ; Dong et al. , 2026 ) . Figure 1: Tool-R0 self-evolution loop. Prior work on LLM agents has proposed different methods to improve their tool-calling skills over vast task datasets (Zhang et al. , 2025c ; Liu et al. , 2025c ; Acikgoz et al. , 2025a ; Qian et al. , 2025 ) . However, curating such datasets through human annotation is inherently labor-intensive and fundamentally unscalable (Huang et al. , 2023 ; Yuan et al. , 2024 ) . As models advance, constructing large and high-quality datasets becomes increasingly unsustainable, creating a scalability bottleneck similar to that observed in LLM pretraining paradigm (Sutskever et al. , 2024 ; Silver and Sutton, 2025 ) . Consequently, reliance on human supervision poses a fundamental scalability bottleneck, limiting the ability of LLM agents to achieve self-evolving capabilities that a superintelligent AI would require (Zhang et al. , 2025b ; Gao et al. , 2025 ) . This naturally raises the following question: Can we empower weak LLMs to improve themselves without requiring human data? Some of the most striking examples of superhuman AI emerge from self-play using RL in game environments (Silver et al. , 2016 ; 2017 ; Bakhtin et al. , 2022 ) . Recently, interest in self-play has re-emerged with the success of LLMs trained via RL; where several works have applied it to reasoning tasks like math (Chen et al. , 2024b ; Cheng et al. , 2024 ; Chen et al. , 2025 ; Zhao et al. , 2025 ; Liu et al. , 2025a ; Huang et al. , 2025 ; Liu et al. , 2025b ) . Despite this progress, these methods do not extend self-play beyond abstract reasoning to tool-using agents in real-world environments. This further motivates whether self-play alone can incentivize LLMs with tool-calling skills across different domains under a zero-data assumption. Recent efforts such as Agent0 (Xia et al. , 2025 ) and Dr. Zero (Yue et al. , 2026 ) have begun to explore this direction. However, their focus remains narrow, either constrained to code tools for math problem-solving or search tools in question answering settings. In contrast, we focus on how to train general-purpose tool-calling agents that can self-evolve base LLMs with zero external data across domains . We propose Tool-R0 , a self-play RL framework for training tool-calling agents that can self-evolve with zero human data ( Fig. 1 ). In Tool-R0 , a single base model is initialized with two different roles: a Generator and a Solver that are trained independently but co-evolve through complementary reward signals. The Generator is rewarded for generating challenging tasks aligned with the Solver ’s evolving capabilities, while the Solver is trained to solve them with outcome-based rewards. To enable effective co-evolution, we design a difficulty-guided reward based on the frozen Solver ’s answer uncertainty, prioritizing tasks at its competence frontier: hard enough to teach and easy enough to verify. We also follow a grounded task synthesis to ensure adaptive and controllable task generation, which conditions the Generator through annotation-free task configurations to prevent mode collapse in free-form generation. Since effective RL requires training data aligned with the model’s current capabilities, we further order the constructed dataset by Solver answer consistency and progressively expose it to harder problems in each batch. Together, this self-evolving cycle significantly improves tool-calling performance over the base LLM and outperforms other supervised baselines across five different tool-calling benchmarks. We summarize the core insights derived from our study as follows: • Self-play can incentivize complex tool-calling skills from zero data. Starting from weak priors, Tool-R0 yields consistent gains across model scales, architectures, and benchmarks, showing that self-play RL alone is sufficient to teach complex tool-calling capabilities. • Adaptive self-generated curricula can surpass static human supervision. Agents trained through self-play generalize better than those trained on fixed human-distribution datasets, demonstrating that LLMs can leverage themselves to identify and address their own capability gaps more effectively than fixed expert-designed curricula. • Role separation is necessary to avoid learning conflicts in self-play. We observe that training Generator and Solver with separate parameters is essential, particularly when both roles operate over high-entropy action spaces with different reward objectives. • Co-evolution with difficulty-aware rewards drives learning. We find that effective learning requires a dynamic Generator guided by smooth difficulty-aware rewards, since static generation or miscalibrated difficulty fails to sustain tool-use improvement. Overview of contributions • (Algorithmic) The primary contribution of this paper is Tool-R0 , an adaptive self-play RL framework, where a Generator and a Solver co-evolve to build general-purpose tool agents with zero data from scratch. • (Performance) Tool-R0 consistently improves base models across architectures, scales, and benchmarks, and surpasses fully supervised tool agents, showing that LLMs can self-evolve into general tool-calling agents in a data-free setting. • (Analysis) We conduct a series of experiments to analyze the key mechanisms of zero-data self-play for tool learning, including: role asymmetry, curriculum difficulty, adversarial co-evolution, self-play as mid-training stage, and scaling behaviors. • (Implementation) We provide a modular and plug-and-play repository of Tool-R0 to support future research on self-evolving agents. 2 Related Work Tool-Learning with LLMs. Tool-learning has emerged as a promising paradigm for extending LLMs capabilities beyond parametric knowledge with external tools (Qu et al. , 2025 ) . It requires LLMs to perform structured reasoning over available tool schemas by selecting the right function, grounding arguments from context, and composing multi-step calls to accomplish goals (Yao et al. , 2023 ; Schick et al. , 2023 ; Patil et al. , 2024 ) . Several benchmarks have been proposed to systematically assess these capabilities across domains, task types, and tool sets (Tang et al. , 2023 ; Srinivasan et al. , 2023 ; Li et al. , 2023 ; Wu et al. , 2024 ) . Building on this foundation, subsequent work has focused on constructing high-quality datasets and fine-tuning LLMs on such data to integrate more complex tool-calling skills (Zeng et al. , 2024 ; Chen et al. , 2024a ; Zhang et al. , 2025c ; Liu et al. , 2025c ; Lin et al. , 2025 ; Acikgoz et al. , 2025a ) , including applying RLVR to further enhance tool-integrated reasoning (TIR) (Jin et al. , 2025 ; Feng et al. , 2025 ; Qian et al. , 2025 ) . However, their dependence on human supervision introduces limitations: (1) static human-curated data leads to distribution shifts that fail to capture the evolving needs of agents during on-policy learning and (2) limits the model’s capabilities to human upper-bound. To the best of our knowledge, Tool-R0 is the first work to leverage self-play RL to train domain-agnostic agents entirely from scratch without any human data , matching the performance of supervised ones, and demonstrating the promise of absolute zero paradigm in LLM agents (Zhao et al. , 2025 ) . Self-Evolution through Self-Play. Self-play has historically driven superhuman performance in closed-loop games (Tesauro, 1995 ; Silver et al. , 2016 ; Bakhtin et al. , 2022 ) . Recently, interest in self-play has re-emerged with the success of LLMs trained via RLVR; where previous work extends self-play to open-domain settings like reasoning (Chen et al. , 2024b ; Cheng et al. , 2024 ; Zhao et al. , 2025 ; Fang et al. , 2025 ; Chen et al. , 2025 ; Liu et al. , 2025a ; Huang et al. , 2025 ; Liu et al. , 2025b ) . These works focus exclusively on abstract reasoning tasks like math, and do not address tool-using agents for real-world applications. More recent studies have begun exploring agentic self-play; Agent0 (Xia et al. , 2025 ) applies self-play with Python coding tools and Dr. Zero (Yue et al. , 2026 ) studies zero-data learning for search-centric question answering. However, these methods remain narrowly scoped in terms of tool-learning, relying on a single tool type and limited action spaces like question answering. Tool-R0 advances this line of work with a general-purpose co-evolutionary framework where LLMs autonomously learn open-ended tool-use across arbitrary domains driven by user needs. We defer a more comprehensive background in Appendix A . Figure 2: Tool-R0 Self-Evolution Framework. A base LLM is initialized into two roles: a Generator and a Solver . The Generator synthesizes challenging tool-calling tasks (question, tool menu, and gold tool-call), targeting the frozen Solver ’s competence frontier with designed rewards ( r fmt + r valid + r curr r_{\text{fmt}}+r_{\text{valid}}+r_{\text{curr}} ). Generated tasks are filtered and ranked easy-to-hard into a curriculum pool. The Solver trains on this curated data to predict tool calls ( r fmt + r acc r_{\text{fmt}}+r_{\text{acc}} ), completing a self-evolving cycle that requires no pre-existing human datasets. 3 Method Overview. We propose Tool-R0 , a zero-data self-play framework for training general-purpose tool-calling agents via dual-RL between two components: a Generator that synthesizes verifiable tool-use tasks, and a Solver that learns to solve them ( Fig. 2 ), both initialized from the same base LLM ( π \pi ). Training proceeds for K K self-play iterations. At each iteration, given domain configurations ( Sec. 3.1 ), we first train Generator ( π θ \pi_{\theta} ) to produce grounded tool-calling tasks that adaptively target the Solver ’s evolving competence frontier ( Sec. 3.2 ). We then freeze the trained Generator and use it to construct a high-quality dataset via deduplication, cross-verification, and difficulty-based curriculum ordering ( Sec. 3.3 ). Finally, the Solver ( π ϕ \pi_{\phi} ) is trained on this curated dataset ( Sec. 3.4 ) and carried forward to the next iteration. This co-evolutionary process drives the Generator to propose progressively harder yet solvable tasks across iterations, while the Solver continuously adapts to an expanding tool-use curriculum; without any human-authored data (full pseudocode in Appendix H ). 3.1 Grounded Task Specification. A key challenge in zero-data self-play is controllable and diverse task generations: when the Generator is prompted only with generic instructions to produce diverse tasks, the underlying language model concentrates probability mass on a small set of high-likelihood patterns, resulting in mode-collapsed generations (Liu et al. , 2025b ) . To prevent this while preserving domain-agnostic adaptability, Tool-R0 grounds task generation using lightweight task specifications s = ( d , c , m , n ) s=(d,c,m,n) where d d denotes the task domain ( Fig. 9 ), c c the interaction context type, m m the number of available tools, and n n the number of gold tool-calls as answer. At each tra